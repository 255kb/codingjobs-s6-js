/**
 * - Register the "DOMContentLoaded" event
 * - display a message in the console when the event is triggered
 *
 * Bonus
 * - increase the amount of content in the page and see what is happening (keep the script at the bottom)
 */

window.addEventListener("DOMContentLoaded", () => {
  console.log("page is fully loaded and ready to be manipulated");

  // we can now start fetching content, modifying it, etc.
});
